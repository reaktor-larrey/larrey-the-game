[package]
name = "pong"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.18.1", features = ["debug"] }
bevy_rand = { version = "0.14", features = ["chacha20", "wyrand"] }
rand = "0.10.0"
